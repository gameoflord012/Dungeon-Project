using System.Collections;
using UnityEngine;

public enum PerformState
{
    // _default shouldn't be used, only use to check if PerformState is initialized
    _default,
    // -------------------------- Actual enums goes bellow -------------------------- //
    succeed,    
    falied
}